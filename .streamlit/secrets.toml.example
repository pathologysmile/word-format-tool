# Streamlit Cloud 密钥配置示例文件
# 复制此文件为 secrets.toml 并填入实际值

# 示例:如果需要添加 API 密钥或其他敏感信息
# [api_keys]
# my_api_key = "your_actual_api_key_here"

# 注意:
# 1. 此文件不应提交到 Git (已在 .gitignore 中)
# 2. 在 Streamlit Cloud 中,可以通过 Settings -> Secrets 直接配置
# 3. 本地开发时,将此文件复制到 .streamlit/secrets.toml
